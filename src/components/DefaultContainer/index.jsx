import styled from "styled-components";

export const Container = styled.section`
    max-width: 80rem;
    margin-inline: auto;

    padding: 4rem 2.5rem;

    @media (max-width: 45rem) {
        padding: 3rem 1.5rem;
    }
`;
